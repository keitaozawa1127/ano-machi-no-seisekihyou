// NOTE:
// このファイルは、Cloudflare Pages での完全静的エクスポート（output: 'export'）対応のため
// いったん無効化されています。Next.js の静的エクスポートでは app/api/ 以下の Route Handler は
// 出力対象外となるため、本番環境ではこのエンドポイントは利用されません。
//
// 動的OGP生成を再度有効化したくなった場合は、Git履歴から元の実装を復元し、
// 静的ホスティング環境に適した形（別Worker等）で提供してください。
